TODO:

FIX (scaling on drop) primitives in Mesh presets

Assign random materials by poly island

select polys by Part
remove parts

fix broken material presets
more environments
more default scenes
add material default scene
faster render presets
item list docked on left, shader tree docked on right (hide/show)
pause render by default
render window as popup palette
F9 render in toolbar
preset browser icons
items list icon
shader tree icon
other kit icons (modes bar)
